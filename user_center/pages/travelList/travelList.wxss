
@import '../../../app.wxss';
page{
  width: 100%;
  background-color: #f4f6f8;
}

.container{
  height: 100vh;
  width: 100%;
  padding: 0 20rpx 40rpx 20rpx;
  box-sizing: border-box;
}

.hiddenBar{
  width: 100%;
  overflow: hidden;
  overflow-y: scroll;
}

::-webkit-scrollbar {
  width: 0;
  height: 0;
  color: transparent;
}

.listBox{
  width: 100%;
  background-color: #fff;
  margin-top: 40rpx;
  padding: 20rpx 30rpx;
  box-sizing: border-box;
  border-radius: 5px;
}

.flex-title {
  display: flex;
  justify-content: space-between;
  color: #333;
  line-height: 80rpx;
  align-items: center;
}

.status{
  color: #999;
}

.iconRight{
  width: 20rpx;
  height: 20rpx;
  display: inline-block;
  border-top: 1px solid #999;
  border-right: 1px solid #999;
  transform: rotate(45deg);
  margin-left: 20rpx;
}

.list-time{
  height: 60rpx;
  align-items: center;
}

.list-time image{
  width: 32rpx;
  height: 32rpx;
  vertical-align: middle;
  margin-right: 10rpx;
}

.time{
  color: #999;
}

.list-adress{
  line-height: 60rpx;
  color: #999;
}

.circle{
  height: 10px;
  width: 10px;
  display: inline-block;
  border-radius: 50%;
  margin: 0 20rpx 0 5rpx;
}

.circle-str{
  background: #3cbca3;
}

.circle-end{
  background: #fc9153;
}

.bg{
  padding: 0 50rpx;
  margin: 216rpx auto 0;
  text-align: center;
  color: #9A9A9A;
  font-size: 28rpx;
}

.bg-img{
  width: 324rpx;
  height: 317rpx;
  margin-bottom: 21rpx;
}

.bg-cont{
  color: #FF8008;
}
.btn-list{
  display: flex;
  flex-direction: row;
  justify-content: flex-end;
  margin-top: 10rpx;
}
.btn-area{
  /* width: 22%; */
  margin-right: 10rpx;
}
.tuik{
  background: #f1c00f;
  color: #fff;
}
.pingjia{
  background: #3cbca3;
  color: #fff;
}
.kaipiao{
  background: #fc9153;
  color: #fff;
}
.popup-box{
  position: absolute;
  z-index: 99;
  top: 0;
  background-color: rgba(0, 0, 0, 0.5);
  width: 100%;
  height: 100%;
}
/* 下 */
.info-bottom{
  position: fixed;
  z-index: 999;
  width: 100%;
  bottom: 0;
  background-color: white;
  border-top-left-radius: 10rpx;
  border-top-right-radius: 10rpx;
}
button{
  margin: 15rpx 0;
}
.row-info{
  display: flex;
  flex-direction: column;
  align-items: center;
  margin: 15rpx;
  font-size: 32rpx;
  height: 200rpx;
}
.row-info view{
  padding: 10rpx 0;
  margin-top: 20rpx;
}
.row-info view:first-child{
  display: flex;
  flex-direction: row;
  align-items: center;
}
.line{
  width: 100rpx;
  height: 1rpx;
}
.left-line{
  background-image: linear-gradient(to left,orange,white);
}
.right-line{
  background-image: linear-gradient(to right,orange,white);
}
.row-author{
  font-size: 24rpx;
  color: gray;
}
.row-btn{
  display: flex;
  flex-direction: row;
  align-items: center;
  border-top: 1rpx dashed #f1f1f1;
}
.row-btn view{
  flex: 1;
  text-align: center;
  margin: 20rpx 10%;
  padding: 12rpx 0;
  font-size: 32rpx;
  border-radius: 10rpx;
}
.left-btn{
  background-color: #f1f1f1;
  color: #33ccff;
}
.right-btn{
  background-color: #33ccff;
  color: white;
}
.fixed{
  position: fixed;
  bottom: 0;
  width: 50%;
}
.yutime{
  font-size: 28rpx;
  color: #777;
  margin-right: 20rpx;
}
.yue{
  position: relative;
  top: -8rpx;
  left: 550rpx;
  width: 100rpx;
  height: 100rpx;
  margin-top: -100rpx;
}
.yuimg{
  width: 100rpx;
  height: 100rpx;
}
.menu-box {
  position: fixed;
  width: 100rpx;
  padding-bottom: 100rpx;
}
.menu-box .navbar {
  z-index: 1000;
  position: absolute;
  bottom: 0rpx;
  width: 100rpx;
  height: 100rpx;
  text-align: center;
  background-color: rgba(0, 0, 0, 0.6);
  color: white;
  font-size: 0rpx;
  border-radius: 50%;
}
.menu-box .navbar>.btn {
  padding: 25rpx;
  width: 100%;
  height: 100%;
  box-sizing: border-box;
}
.menu-box .navbar>.btn>.line1 {
  height: 6rpx;
  background-color: white;
  margin: 0px 2rpx 6rpx 2rpx;
  border-radius: 6rpx;
}
.menu-box .navbar>.btn>.title {
  font-size: 20rpx;
}
.menu-box .active {
  background-color: pink;
}
.menu-box .menu{
  /* width: 100rpx; */
  height: 100rpx;
  /* overflow: hidden; */
  position: relative;
  /* margin: 0 auto; */
  bottom: -100rpx;
}
.menu-box .menu>view {
  z-index: 100;
  background-color: rgba(0, 0, 0, 0.6);
  margin-bottom: 20rpx;
  border-radius: 50%;
  height: 100rpx;
  line-height: 100rpx;
  position: absolute;
  width: 100rpx;
  height: 100rpx;
  margin-left: auto;
  margin-right: auto;
  opacity: 0;
  font-size: 0rpx;
  color: red;
}
.menu-box .menu>view text{
  display: inline-block;
  position: absolute;
  text-align: center;
  width:100%;
  height: 100%;
  color: white;
  z-index: 10000;
  font-size: 25rpx;
}
.menu_box{
  display: flex;
  height: 80rpx;
}
.menu1,.menu2{
  flex: 1;
  font-size:30rpx;
  line-height: 80rpx;
  text-align: center;
}
.borders{
  border-bottom: 4rpx solid #f00;
  color: #f00;
}
.chenge_time {
  display: flex;
    justify-content: flex-end;
    align-items: center;
}
.pickers {
  background-color: #1aad19;
  color: #fff;
  padding: 0 20rpx;
  height: 64rpx;
  line-height: 64rpx;
  border-radius: 15rpx;
  font-size: 26rpx;
}
/* 吸底栏样式 */
/* .sticky-bottom-bar {
    position: fixed;
    bottom: 0;
    left: 0;
    right: 0;
    height: 100rpx;
    display: flex;
    align-items: center;
    padding: 0 30rpx;
    background-color: #ffffff;
    box-shadow: 0 -2rpx 10rpx rgba(0, 0, 0, 0.1);
    z-index: 999;
    align-self: start;
    margin-bottom: env(safe-area-inset-bottom);
  }

  .btm_box {
    flex: 1;
    display: flex;
    justify-content: space-around;
  }
  .goinvoice {
   width: 150rpx;
   background-color: #1aad19;
   border-radius: 10rpx;
   color: #fff;
   padding: 5rpx 10rpx;
   display: flex;
   align-items: center;
   justify-content: center;
  } */

  /* 1. 触发按钮样式 */
.trigger-btn {
  padding: 20rpx 30rpx;
  background-color: #f7f8fa;
  border-radius: 10rpx;
  font-size: 28rpx;
  color: #333;
}

/* 2. 全屏半透明黑色遮罩 */
.custom-picker-mask {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background-color: rgba(0, 0, 0, 0.5); /* 50%透明度黑底 */
  z-index: 999; /* 确保在最顶层 */
  display: flex;
  align-items: flex-end; /* 让内容靠底部对齐 */
}

/* 3. 底部弹出的白色圆角容器 */
.custom-picker-container {
  width: 100%;
  max-height: 40vh; /* 限制最大高度为屏幕60%，防止数据过多撑满屏幕 */
  background-color: #fff;
  border-radius: 24rpx 24rpx 0 0; /* 顶部左右圆角 */
  overflow: hidden;
  display: flex;
  flex-direction: column;
}

/* 4. 顶部操作栏 (取消/确定) */
.picker-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 24rpx 32rpx;
  border-bottom: 1rpx solid #eee;
}

.picker-header text {
  font-size: 30rpx;
}

.picker-header text:first-child {
  color: #999; /* 取消按钮颜色 */
}

.picker-header text:last-child {
  color: #07c160; /* 微信绿主题色 */
  font-weight: bold;
}

/* 5. 核心：可滚动的列表区域 */
.picker-list {
  flex: 1; /* 占据剩余所有高度 */
  height: 200rpx; /* 给一个固定的初始高度用于滚动计算 */
  box-sizing: border-box;
  overflow-y: auto;
  margin-bottom: 20rpx;
}

/* 6. 单个列表项样式 */
.picker-item {
  padding: 24rpx 32rpx;
  font-size: 30rpx;
  color: #333;
  text-align: center;
  border-bottom: 1rpx solid #f5f5f5;
  transition: all 0.2s ease; /* 增加点击反馈过渡效果 */
}

/* 7. 选中状态的高亮样式 */
.picker-item.active {
  background-color: #f7f8fa;
  color: #07c160;
  font-weight: bold;
}

/* 8. 按下时的触觉反馈（可选优化） */
.picker-item:active {
  background-color: #eaeaea;
}